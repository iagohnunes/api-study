# =============================================================================
# DATABASE
# =============================================================================
# PostgreSQL connection string (Supabase ou local)
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/mydb?schema=public"

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# Access Token (token principal - curta duração)
# IMPORTANTE: Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_minimum_64_characters_use_crypto_randomBytes
JWT_EXPIRES_IN=5m

# Refresh Token (renova access token - longa duração)
# IMPORTANTE: Use secret DIFERENTE do JWT_SECRET
JWT_REFRESH_SECRET=your_refresh_secret_here_different_from_jwt_secret_64_chars
JWT_REFRESH_EXPIRES_IN=3d

# =============================================================================
# APPLICATION
# =============================================================================
PORT=3000
NODE_ENV=development

# =============================================================================
# INSTRUÇÕES
# =============================================================================
# 1. Copie este arquivo para .env
#    cp .env.example .env
#
# 2. Preencha os valores reais (não commite o .env!)
#
# 3. Para gerar JWT secrets seguros:
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# 4. Configure seu banco de dados (Supabase ou PostgreSQL local)
#
# 5. Execute as migrations:
#    npx prisma migrate dev
#
# 6. Execute os seeds:
#    npx prisma db seed
